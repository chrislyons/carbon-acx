# Carbon ACX Environment Variables
# Copy this file to .env and customize for your local environment
# DO NOT commit .env files - they are gitignored for security

# ============================================================================
# Data Backend Configuration
# ============================================================================

# ACX_DATA_BACKEND
# Purpose: Specifies which data backend to use for derivation pipeline
# Options: "csv" (default) or "sqlite"
# Default: csv
# ACX_DATA_BACKEND=csv

# ============================================================================
# Build Output Configuration
# ============================================================================

# ACX_OUTPUT_ROOT
# Purpose: Root directory for derivation pipeline outputs
# Default: dist/artifacts (automatically set by Makefile)
# ACX_OUTPUT_ROOT=dist/artifacts

# ACX_ARTIFACT_DIR
# Purpose: Directory for reading artifacts in Dash app
# Default: calc/outputs (local dev) or dist/artifacts (production)
# Used by: app/app.py
# ACX_ARTIFACT_DIR=

# ============================================================================
# Dataset Metadata
# ============================================================================

# ACX_GENERATED_AT
# Purpose: ISO 8601 timestamp for dataset generation
# Format: YYYY-MM-DDTHH:MM:SS+00:00
# Default: 1970-01-01T00:00:00+00:00 (Makefile sets this)
# Used by: calc/derive.py for manifest timestamps
# ACX_GENERATED_AT=

# ============================================================================
# Static Site Configuration
# ============================================================================

# PUBLIC_BASE_PATH
# Purpose: Base path for Vite static site (for subdirectory deployments)
# Default: / (root)
# Example: /carbon-acx/ (if deployed to subdirectory)
# Used by: site/vite.config.ts
# PUBLIC_BASE_PATH=/

# ============================================================================
# Node.js & Build Configuration
# ============================================================================

# NODE_ENV
# Purpose: Node.js environment mode
# Options: development, production, test
# Default: development
# NODE_ENV=development

# ============================================================================
# Cloudflare Workers Configuration
# ============================================================================

# ACX_DATASET_VERSION
# Purpose: Version identifier for deployed dataset
# Default: dev (local), set via wrangler.toml for production
# Used by: workers/compute/index.ts
# ACX_DATASET_VERSION=dev

# ============================================================================
# Developer Notes
# ============================================================================
#
# Most environment variables are optional and have sensible defaults.
# The Makefile automatically sets ACX_OUTPUT_ROOT and ACX_GENERATED_AT
# during builds, so you typically don't need to set them manually.
#
# For local development, you usually only need:
# - ACX_DATA_BACKEND (if you want to test SQLite backend)
# - PUBLIC_BASE_PATH (if testing subdirectory deployments)
#
# See README.md for more information on build workflows.
# See CLAUDE.md for detailed environment variable documentation.
